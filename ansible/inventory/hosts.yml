---
# Mirrors hosts/default.nix + hosts/loader.nix: each host is "<user>@<hostname>" configured locally.
# Group membership replaces the loader imports: 0-type-<type>.nix -> group, 1-system-<os>.nix -> group.
all:
  vars:
    ansible_connection: local
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
  children:
    desktop:
      hosts:
        M-PA-LT75QJ7NN7:
        mbp:
    darwin:
      hosts:
        M-PA-LT75QJ7NN7:
        mbp:
